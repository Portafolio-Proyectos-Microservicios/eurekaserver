server:
  port: 8761

spring:
 application:
  name: eurekaserver
 banner:
  location: classpath:banner/banner.txt

# Config Server
 config:
  import: "configserver:"
 cloud:
 cloud.config.uri: http://configserver:8888